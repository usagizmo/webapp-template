{
  "name": "nextjs-template",
  "author": "usagizmo <usagizmo@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build --scope \"@nextjs-template/{app,mock}\"",
    "test": "lerna run test --scope \"@nextjs-template/app\"",
    "format": "lerna run format --scope \"@nextjs-template/{app,mock,postcss}\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "cross-env": "^7.0.2",
    "lerna": "^3.22.1",
    "onchange": "^7.1.0",
    "prettier": "^2.2.0"
  }
}
