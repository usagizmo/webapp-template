{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "generate": {
      "inputs": ["$TURBO_DEFAULT$", "supabase/**"],
      "outputs": ["$generated/types.ts"]
    },
    "test": {
      "cache": false
    }
  }
}
