{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "generate": {
      "inputs": ["$TURBO_DEFAULT$", "supabase/**", "!$generated/types.ts"],
      "outputs": ["$generated/types.ts"]
    },
    "test": {
      "cache": false
    }
  }
}
