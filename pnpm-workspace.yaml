# pnpm workspace configuration
packages:
  # Applications
  - apps/*
  # Shared packages
  - packages/*

# Allow these packages to run build scripts for security
onlyBuiltDependencies:
  - '@parcel/watcher'
  - '@sveltejs/kit'
  - '@tailwindcss/oxide'
  - esbuild
  - supabase
