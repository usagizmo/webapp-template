{
  "name": "@repo/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./supabase": {
      "types": "./dist/supabase.d.ts",
      "default": "./dist/supabase.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "generate": "node scripts/copy-supabase-types.js",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests for types package'",
    "lint:cspell": "cspell \"**\"",
    "lint:prettier": "prettier . --check --ignore-path=../../.prettierignore",
    "lint": "concurrently pnpm:lint:*",
    "format:prettier": "prettier . --write --ignore-path=../../.prettierignore",
    "format": "concurrently pnpm:format:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
