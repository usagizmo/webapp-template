{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "web#generate": {
      "outputs": ["schema.graphql"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "mockup#build": {
      "dependsOn": ["^build"],
      "outputs": ["public/styles.css"]
    },
    "web#build": {
      "dependsOn": ["^build", "generate"],
      "outputs": [".svelte-kit/**", "$houdini/**", "schema.graphql"]
    },
    "dev": {
      "cache": false
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "mockup#test": {
      "outputs": ["tests/external-links.txt"]
    }
  }
}
